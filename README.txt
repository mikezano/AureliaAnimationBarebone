Steps to run after cloning/downloading

jspm install
npm install
npm install gulp
gulp watch

To install the aurelia spotify library, had to:
jspm install github:Vheissu/aurelia-spotify  (it should be part of the jspm now)
